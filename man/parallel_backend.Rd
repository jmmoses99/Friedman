% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel_processing_backend.R
\name{parallel_backend}
\alias{parallel_backend}
\title{Choose a parallel backend for IDPR processing}
\usage{
parallel_backend(cores = parallel::detectCores() - 1)
}
\arguments{
\item{cores}{Number of cores to use (default = detected cores - 1). Minimum is 1.}
}
\value{
A \code{BiocParallelParam} object suitable for use in parallel Bioconductor workflows.
}
\description{
Automatically selects the appropriate BiocParallel backend depending on the operating system:
\itemize{
\item On macOS, uses \code{MulticoreParam}.
\item On Windows or other OS, uses \code{SnowParam} (SOCK cluster).
}
}
\details{
This ensures consistent parallel processing behavior across platforms for functions like
\code{idpr_parallel_processing()}.
}
\examples{
\dontrun{
BPPARAM <- IDPsBio::parallel_backend(cores = 4)
results <- idpr_parallel_processing(
  idpr_functions = c("iupred", "scaledHydropathyLocal"),
  input = P53_Human_Protein,
  BPPARAM = BPPARAM
)
}

}
